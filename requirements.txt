Flask>=2.0.0
Flask-CORS>=3.0.0
Flask-SocketIO>=5.0.0
eventlet>=0.30.0
# Python-dotenv can be useful for managing .env files if preferred for local dev,
# but current config.py setup uses direct os.getenv.
# python-dotenv>=0.19.0
